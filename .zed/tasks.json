[
  {
    "label": "Tests: pytest",
    "command": "python -m pytest -q",
    "use_new_terminal": false,
    "reveal": "always"
  },
  {
    "label": "Environment: verify audio stack",
    "command": "python -c \"from importlib.metadata import version; import librosa, numpy, soxr, torch; from torchcodec.decoders import AudioDecoder; print(f'torch={torch.__version__}'); print(f'torchcodec={version(\\\"torchcodec\\\")}'); print(f'librosa={librosa.__version__}'); print(f'numpy={numpy.__version__}'); print(f'soxr={soxr.__version__}'); print(f'decoder={AudioDecoder.__name__}')\" && ffmpeg -version | sed -n '1p'",
    "use_new_terminal": false,
    "reveal": "always"
  }
]
